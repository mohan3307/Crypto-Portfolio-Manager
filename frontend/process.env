const API = axios.create({
  baseURL: `${process.env.REACT_APP_API_URL}/api`
});
```

Or update the env variable on **Vercel → Frontend → Settings → Environment Variables**:
```
REACT_APP_API_URL = https://crypto-portfolio-manager-ufz4.vercel.app/api